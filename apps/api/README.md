# CRM API

FastAPI backend for the CRM. See the repository root README for how to run the
whole stack, and `docs/CRM_BACKEND_STRUCTURE.md` for the intended module layout.

Sprint 0 delivers the scaffold: app factory, settings, async database wiring,
`/health` and `/ready`, and one Alembic revision. Business modules land from
sprint 1.
